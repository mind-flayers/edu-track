import 'package:flutter/material.dart';

class AddExamResultsScreen extends StatelessWidget {
  const AddExamResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Exam Results'),
      ),
      body: const Center(
        child: Text('Add Exam Results Screen Placeholder'),
      ),
    );
  }
}